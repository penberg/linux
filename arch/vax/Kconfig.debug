menu "Kernel hacking"

source "lib/Kconfig.debug"

config EARLY_PRINTK
	bool "Early printk"
	default y
	---help---
	  Write kernel log output directly to the serial console.

	  This is useful for kernel debugging when your machine crashes very
	  early before the console code is initialized.

endmenu
